# Freshet — Roadmap

## 🚀 Shipped

- **v1.0.0** — submitted 2026-04-19, approved 2026-04-21. Initial CWS launch.
- **v1.1.0** — submitted 2026-04-21. Top-strip polish (right-cluster layout, theme dropdown, `rule` / `template` kind prefixes, live `<html data-theme>` updates), rule-card a11y (WCAG AA contrast, nested-interactive resolved), E2E baseline restored to 44/44, refreshed CWS screenshots, new 440×280 promo tile.
- **v1.2.0** — built 2026-04-25. Drag-to-reorder rule cards (left-side ⋮⋮ grip; insertion slot + lifted card; live target numbering; auto-scroll near edges; Escape-cancel; `prefers-reduced-motion` aware; polite `aria-live` on drop). Template debugging starter — `__root` engine handle, `tree` filter, `json-debug` starter template, debug docs at `/debug/`. Editor `Debugging templates` link. CodeMirror caret + selection contrast fixed in dark mode. Hand-rolled Pointer Events (no new deps).

Install: <https://chromewebstore.google.com/detail/freshet/mpclplhdencffbilobpcapccnihpelcg>

## 🕓 Awaiting review — v1.2.0

v1.2.0 zip + refreshed Rules-tab screenshot ready for upload at handoff. Listing freeze re-engages once submission is in flight — don't edit listing fields, upload new zips, or resubmit while the review badge is up. If bounced: read the reviewer note, fix surgically, bump to v1.2.1, rebuild + re-zip, resubmit per the `feedback_release_process` memory.

## 🔮 Future work

Tracked in [**GitHub Issues**](https://github.com/MattAltermatt/freshet/issues). File new issues there; add milestones as they take shape.
